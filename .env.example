# Copy to .env and fill in. NEVER commit the real .env.
# Sepolia deployment (do not deploy without review — see CLAUDE.md)
SEPOLIA_RPC_URL=
DEPLOYER_PRIVATE_KEY=
ETHERSCAN_API_KEY=

# Backend / MySQL (used later by backend/)
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=certverify
CONTRACT_ADDRESS=
